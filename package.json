{
  "name": "documents",
  "version": "1.0.0",
  "description": "Documents of Norden Wind Ensemble",
  "scripts": {
    "lint": "textlint docs/** && prettier --check .",
    "lint:fix": "textlint --fix docs/** && prettier --write .",
    "build": "md-to-pdf --config-file md-to-pdf.config.json docs/*.md",
    "prepare": "husky"
  },
  "author": "Norden Wind Ensemble",
  "license": "MIT",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.2",
    "prettier": "^3.4.2",
    "prettier-plugin-md-nocjsp": "^1.5.1",
    "textlint": "^14.4.2",
    "textlint-rule-preset-jtf-style": "^3.0.1"
  },
  "dependencies": {
    "md-to-pdf": "^5.2.4"
  }
}
